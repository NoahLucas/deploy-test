import Foundation

enum SiteConfiguration {
    static let websiteURL = URL(string: "https://noahlucas.com")!
    static let websiteHost = "noahlucas.com"
    static let apiBaseURL = URL(string: "http://127.0.0.1:8000")!
}
